# Core dependencies for unified PDF processing pipeline
spacy>=3.7.4,<3.8.0  # Pinned to avoid NumPy 2.0 compatibility issues
scispacy>=0.5.0,<0.5.6  # Compatible with spacy<3.8.0
https://s3-us-west-2.amazonaws.com/ai2-s2-scispacy/releases/v0.5.4/en_core_sci_md-0.5.4.tar.gz  # SciSpacy English medium model
litellm
# nmslib>=2.1.1, install using mamba install -c conda-forge nmslib
scikit_learn>=1.7.1
scipy>=1.13.0,<1.14.0  # Compatible with NumPy 1.26.x
dspy>=2.6.0
pandas>=2.3.0
requests>=2.32.0
tqdm>=4.66.0
chromadb>=0.4.0  # For entity linking with vector similarity search

# Additional dependencies that may be needed
numpy>=1.24.0,<2.0.0  # Pinned to 1.x for spaCy/scispacy compatibility
thinc>=8.2.0,<8.3.0  # Compatible with NumPy 1.x
--extra-index-url https://download.pytorch.org/whl/cu124
torch==2.6.0+cu124
torchvision==0.21.0+cu124
torchaudio==2.6.0+cu124
transformers>=4.41.0
accelerate>=0.33.0
safetensors>=0.4.2
sentencepiece>=0.2.0
huggingface_hub>=0.23.0
tokenizers>=0.19.0
einops>=0.8.0

# For async operations
aiohttp>=3.12.0
asyncio

# For JSON handling
ujson>=5.10.0

# For logging and utilities
colorlog>=6.9.0
rich>=14.0.0

# For cloud deployment
python_dotenv>=1.0.0

# Web API dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python_multipart>=0.0.6 